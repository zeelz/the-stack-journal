---
import Cobe from "@components/cobe.astro";
import Link from "@components/ui/link.astro";
import abstractDottedGlobeIllustration from "assets/abstract-dotted-globe-illustration.png";
import { Image } from "astro:assets";
import { t } from "i18next";

import Video from "@assets/tsj-video.mp4";

const { dynamic = false } = Astro.props;
---

<div class="relative isolate dark:bg-stone-950 md:h-screen bg-[#000]">
	<!-- <video controls width="auto" style="height: 100%; margin: 0 auto">
		<source src={Video} type="video/mp4" />
		 Download the<a href="/media/cc0-videos/flower.webm">WEBM</a>or<a href="/media/cc0-videos/flower.mp4">MP4</a>
		 video.
	</video> -->

	<section class="wrap min-h-[400px] md:h-screen">
		<div class="video-bg">
			<!-- autoplay=0 -->
			<iframe
				src="https://www.youtube.com/embed/3wjlKBuOqUE?autoplay=1&mute=1&playsinline=1&loop=1&controls=0&disablekb=1&rel=0"
				frameborder="0"
				allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
				allowfullscreen
				class="md:w-screen md:h-screen md:top-1/2 md:left-1/2 md:translate-x-1/2 md:translate-y-1/2">
			</iframe>
		</div>

		<!-- <div class="content">
			<div class="container bg-[red]">
				<div class="w-[50px] h-[10px] bg-[red] fixed inset auto auto 0">
					<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
						<path stroke-linecap="round" stroke-linejoin="round" d="M19.5 5.25l-7.5 7.5-7.5-7.5m15 6l-7.5 7.5-7.5-7.5"></path>
					</svg>
				</div>
			</div>
		</div> -->
	</section>
</div>

<style>
	@keyframes slide {
		0% {
			left: calc(50% - 70rem);
			transform: rotate(0deg);
		}
		50% {
			left: calc(50% - 30rem);
			transform: rotate(-180deg);
		}
		100% {
			left: calc(50% - 70rem);
			transform: rotate(0deg);
		}
	}

	.animate-slide {
		animation: slide 25s infinite;
	}

	/* forr video */

	html,
	body {
		margin: 0;
		padding: 0;
	}

	.wrap {
		position: relative;
		/* height: 100vh; */
	}

	.video-bg {
		position: relative;
		height: 100%;
		background: #000;
		overflow: hidden;
	}

	.video-bg::after {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		display: block;
		width: 100%;
		height: 100%;
		background: rgba(0, 0, 0, 0.25);
		/* background-image: url("data:image/svg+xml,%3Csvg width='6' height='6' viewBox='0 0 6 6' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23000000' fill-opacity='0.4' fill-rule='evenodd'%3E%3Cpath d='M5 0h1L0 6V5zM6 5v1H5z'/%3E%3C/g%3E%3C/svg%3E"); */
	}

	.video-bg iframe {
		display: block;
		position: absolute;

		top: 0;
		/* left: 50%; */
		/* transform: translate(-50%, -50%); */
		/* finish this later */
		/* ultimately mobile should have 500px for video page */
		width: 100%;
		height: 100%;
		/* min-height: 200px; */
	}

	@media (min-width: 768px) {
		.video-bg iframe {
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%);
			width: 100vw;
			height: 100vh;
		}
	}

	.content {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		color: #fff;
	}

	.container {
		position: fixed;
		/* display: block; */
		bottom: 5%;
		left: 0;
		/* right: 0; */
		width: 100%;
		transform: translate(-50%, -50%);
	}

	.title {
		text-align: center;
		font-size: 2.5vw;
	}
</style>
